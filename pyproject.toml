[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ddm"
version = "0.1.0"
description = "Decision drift diffusion modeling tools"
authors = [{name = "Brayden Chien"}]
dependencies = []

[tool.setuptools]
packages = ["ddm"]